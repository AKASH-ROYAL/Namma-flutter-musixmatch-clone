import 'package:musixmatch_clone_namma_flutter/app/config/export.dart';

class Identify extends StatelessWidget {
  const Identify({super.key});

  @override
  Widget build(BuildContext context) {
    return Material(
      child: Center(child: NectText(text: "Identify")),
    );
  }
}
